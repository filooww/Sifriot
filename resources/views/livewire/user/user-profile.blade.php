<div class="py-12">
    <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
        <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
            <div class="p-6 text-gray-900 dark:text-gray-100">
                <h2 class="text-2xl font-bold mb-4">{{ __('Your Profile') }}</h2>
                <p class="text-gray-600 dark:text-gray-400 mb-6">
                    {{ __('Your profile page. Future features: Bookmarks, Likes, Comments, Settings') }}
                </p>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <!-- Placeholder Cards -->
                    <div class="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
                        <h3 class="text-lg font-semibold mb-2">{{ __('Bookmarks') }}</h3>
                        <p class="text-sm text-gray-500 dark:text-gray-400">{{ __('Coming soon') }}</p>
                    </div>

                    <div class="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
                        <h3 class="text-lg font-semibold mb-2">{{ __('Likes') }}</h3>
                        <p class="text-sm text-gray-500 dark:text-gray-400">{{ __('Coming soon') }}</p>
                    </div>

                    <div class="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
                        <h3 class="text-lg font-semibold mb-2">{{ __('Comments') }}</h3>
                        <p class="text-sm text-gray-500 dark:text-gray-400">{{ __('Coming soon') }}</p>
                    </div>

                    <div class="p-6 border border-gray-200 dark:border-gray-700 rounded-lg">
                        <h3 class="text-lg font-semibold mb-2">{{ __('Settings') }}</h3>
                        <p class="text-sm text-gray-500 dark:text-gray-400">{{ __('Coming soon') }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
